# samples/

Drop ONE real IELTS test HTML file here (any of the existing ready-made tests).
The import parser (BRIEF §4.2 / §4.2.1) is written against the actual file
structure, so it needs a real sample to be exact.

Raw test HTML/audio is client-owned content (BRIEF §11) and is **gitignored** —
it won't be committed to the repo.
