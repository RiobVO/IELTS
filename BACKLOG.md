# BACKLOG.md — продуктовый бэклог «20 → 100»

> Назначение: приоритезированный список продуктовых ходов из живого ревью
> (5 шляп: PM / UX / Growth / Senior Eng / IELTS-методист) — что **добавить**,
> **улучшить**, сделать **иначе**, чтобы продукт стал уровня «100, а не 20».
> Это НЕ операционный чеклист запуска (он в [LAUNCH.md](./LAUNCH.md)) и НЕ спека
> (она в [BRIEF.md](./BRIEF.md)). Источник правды по фазам — `CLAUDE.md §Status`.
>
> **Контракт:** работаем волнами, один пункт = один подтверждённый шаг (как в
> [WORKLOG.md](./WORKLOG.md) §5). Перед началом пункта — план + acceptance, ждать
> «делай». НИЧЕГО не ломать (grading/submit/RLS/tiers/рейтинг идентичны). Стиль:
> inline-стили + токены, ноль новых runtime-зависимостей.

## Как читать

- Статусы: `☐` не начато · `🔄` в работе · `✅` закрыто (дата + 1 строка «что сделано»).
- Категория: `[ADD]` добавить · `[IMPROVE]` улучшить · `[DIFFERENTLY]` сделать иначе/убрать.
- Усилие: `S` (часы) · `M` (день-два) · `L` (отдельная волна).
- Каждый пункт привязан к метрике: activation / retention / revenue (free→paid) /
  виральность / рост балла.
- Провенанс: находки из живого прохода прод-сайта `ielts-rho-seven.vercel.app`
  (2026-06-18), вход Basic-юзером. Не выдумано по коду — увидено вживую, кроме
  явно помеченных гипотез.

## Скоуп-граница (что НЕ в этом файле)

- Деплой / ключи / контент в live БД / Lighthouse-замеры → `LAUNCH.md`.
- **Mobile / responsive** `/app` — редизайн раннера закрыт раньше (`cb41db2`+`60055e7`,
  память `mobile-exam-runner-redesign`); mobile release-gate (3 golden path на реальном
  устройстве) пройден предзапусковой волной 2026-07-11 (BRIEF §12.3, 2 найденных major
  зачинены). Долг закрыт — не структурный must на будущее, а разовая проверка перед T0.

---

## Волна 1 — двигает деньги сейчас (по «влияние ÷ усилие») — ✅ ЗАКРЫТА (2026-06-18)

> Семь ходов, по одному на звено воронки: acquire → activate → convert → retain →
> viral → trust → protect. Докидывать в то же звено — убывающая отдача, пока эти не выкатятся.

> **✅ Статус: все 7 пунктов закрыты + запушены в `main`, прод проверен вживую.**
> Коммиты: W1-6 EN-чистка+`/pricing` (`cea76c4`, `3f60fa8`) · W1-1+3 пейволл (`058ffee`) ·
> W1-2 онбординг + миграция `0012` (`26279d6`) · W1-4 band + копирайт (`d19046d`) ·
> W1-7 анти-бот seam (`bd7f1e3`) · W1-5 шеринг (`9b047e0`) · W1-2b мини-диагностика (`723096f`).
> Миграции `0011` (бейджи→EN) + `0012` (`onboarded_at`) применены на Supabase.
>
> **Вынесенные под-части (НЕ блокируют закрытие волны):**
> - **W1-2b мини-диагностика** (зашитый пассаж + 6 вопросов в онбординге) — ✅ сделано (`723096f`).
> - **W1-5b student Telegram-бот** (тест дня / стрик-напоминания / мини-апп / image band-карточка) —
>   ⏸ **ОТЛОЖЕНО: вернёмся после Волн 2–3.** Требует отдельный бот-токен + scheduler + image-стек
>   (или новую runtime-зависимость). Виральный шеринг результата (t.me/share + реф-ссылка) уже на проде.
> - **Анти-бот** — код-seam готов (Turnstile, fail-open), активируется Cloudflare-ключами когда нужно.

- `✅` **W1-1 · [DIFFERENTLY] · PM/Методист — Перенести пейволл: per-type breakdown
  бесплатно, платно — глубина.** _(2026-06-18, `058ffee`: breakdown + ✓/✗ по вопросам — free; ответы/объяснения/evidence — Premium, не утекают в HTML.)_
  Сейчас Basic после теста видит голый `%` («23% · single passage») + стену
  «Full review is on Premium». Заперты: разбор по типам, объяснения, evidence.
  · Почему: разбор по типам — это «aha» и весь оффер лендинга; запирая его, убиваем
  активацию И противоречим брифу (§4.8: Basic = «базовый разбор» + «аналитика по типам
  7 дней», а не голый %). Free должен **почувствовать** ценность; платим за объяснения +
  evidence + история (d/w/m) + Full-моки. Чинит и разорванную учебную петлю (методист:
  без «почему ответ неверный» free не учится, а угадывает — ровно то, что продукт лечит).
  · Усилие: M (логика гейта уже есть, `src/lib/tiers.ts`). · Метрика: activation + free→paid.

- `✅` **W1-2 · [ADD] · PM — Онбординг-диагностика 5–7 вопросов + захват
  target band / display_name / region.** _(2026-06-18, `26279d6` захват identity + гейт + миграция `0012`; `723096f` мини-диагностика.)_
  После signup сразу дашборд с пустым band; target band = «Not set», имя/регион не собираются.
  · Почему: занятому UZ-студенту с телефона нельзя ставить «20–60 мин полного теста»
  порогом до первой ценности — мини-микс 5–7 вопросов даёт «твой слабый тип» за ~5 мин.
  Захват target band разблокирует нарративу лендинга «дистанция до 7.0»; display_name+region
  разблокируют регион-лиги (см. W1-7) и убирают «Anonymous». · Усилие: M. · Метрика: activation / D1.

- `✅` **W1-3 · [DIFFERENTLY] · UX — Result-страница как урок, не тупик.** _(2026-06-18, `058ffee`: «What you missed» с ✓/✗ по вопросам для free, объяснения за Premium.)_
  Free видит `%` + 2 кнопки (upgrade / try again); нельзя посмотреть даже **какие**
  ответы неверны. · Почему: эмоциональный пик IELTS-разбора заперт целиком. Дать free
  «верно/неверно по каждому» + per-type бары на этой же странице + CTA «дрилл этих ошибок»;
  объяснения/evidence — за Premium. Превращает момент истины в возврат, а не в пейволл-стену.
  · Усилие: M. · Метрика: result→next-action.

- `✅` **W1-4 · [IMPROVE] · PM/Методист — Починить пустой band (заглавную метрику).** _(2026-06-18, `d19046d`: три честных состояния band + CTA на Full mock.)_
  «Best band —» на профиле; дашборд пишет «Take a test to see your band» даже после
  пройденных тестов. Band есть только у Full-тестов (1 бесплатный); дрилл-юзер его не видит
  никогда. · Почему: главная обещанная цифра невидима. Варианты: прогноз band по накопленным
  single-passage ИЛИ явный «band появится после Full mock» + push на бесплатный Full
  (методически честно не выдумывать band из 13 вопросов — но тогда сказать это прямо).
  · Усилие: S–M. · Метрика: Full-test starts / retention.

- `🔄` **W1-5 · [ADD] · Growth — Student-facing Telegram + шеринг band-карточки.** _(2026-06-18, `9b047e0`: шеринг результата в Telegram (t.me/share + реф-ссылка) ✅. Полный бот → W1-5b, **отложен после Волн 2–3**.)_
  Сейчас Telegram только админский (импорт контента). Шарить результат нечем.
  · Почему: в UZ/ЦА Telegram — канал №1; «тест дня», напоминание о стрике, шеринг
  брендовой карточки «band + слабый тип» в чат/сторис → друг переходит. Органика + виральность,
  CAC ≈ 0. Мини-апп может хостить тот же exam-раннер. · Усилие: L. · Метрика: organic signups / K-factor.

- `✅` **W1-6 · [IMPROVE/DIFFERENTLY] · UX/Growth — Локализация-чистка (всё EN) +
  публичный `/pricing`.** _(2026-06-18, `cea76c4`+`3f60fa8`: все UI-строки EN (бейджи via миграция `0011`), публичный `/pricing` в guest-режиме, честный реферал-копирайт.)_
  Бейджи (названия+описания), уведомления, табы лидерборда («Эта неделя/Practise») —
  русские при английском UI; форматы дат смешаны (17.06.2026 vs 6/17/2026). Нав/футер
  «Pricing» кидают в /auth — публичной цены нет. · Почему: RU-вкрапления читаются как
  недоделка не-носителям; спрятанная цена = выше bounce у ценочувствительной аудитории.
  Свести строки к EN (i18n отложен — это просто английские строки, не фреймворк); вынести
  готовую таблицу тарифов из `/app/upgrade` на публичный `/pricing`. · Усилие: S. · Метрика: bounce / signup-rate / trust.

- `✅` **W1-7 · [DIFFERENTLY] · Eng/Growth — Анти-бот gate перед масштабированием
  реферал-награды + сбор identity (→ лидерборд про людей).** _(2026-06-18, `bd7f1e3`: Turnstile-seam (fail-open, ждёт Cloudflare-ключей); identity в лидерборде уже работала — разблокирована онбордингом W1-2.)_
  Лидерборд: все «Anonymous», регион-фильтр (§4.6) в UI не выведен. Реферал-награда в UI —
  «one week of Premium» (см. гипотезу H1). · Почему: если награда реально Premium, фарм
  мульти-аккаунтами становится дорогим, а анти-бот на signup открыт → прямой слив Premium.
  Связать в один gate: анти-бот (нужны Cloudflare-ключи, см. `LAUNCH.md §8`) + сбор
  display_name/region на signup (бэкфилл позже — боль) + региональные лиги. · Усилие: M
  (зависит от внешних ключей). · Метрика: fraud-rate / лидерборд-engagement.

---

## Волна 2 — углубляет продукт и ров (вторым фронтом)

> Реально названо в ревью, но проигрывает Волне 1 по усилию/моменту. Не «идеи ради списка».
>
> **Статус (2026-07-11): 7/8 закрыто** (нумерованные W2- + OPS-1 вне номера). W2-1/W2-2
> закрыты ранее (редизайн + фаза 3 Practice — бэклог отставал от `main`), W2-4/W2-6/W2-7 —
> волной 2026-07-07, W2-5 — волной «учебная петля» 2026-07-08, W2-3b — предзапусковой
> волной 2026-07-11 (QTYPE hard-block), OPS-1 — той же волной (Storage-чистка). Открыт
> только **W2-3** (контент-движок: ритм пополнения + витрина «новое» — процессный/
> контентный, не чисто-кодовый). Email-блок (вне W2-нумерации) — закрыт целиком 2026-07-08:
> код+полка владельца (07-07), активация Brevo + доводка чеклиста (07-08) — BRIEF §12.1 п.2.
>
> **Статус (2026-08-11):** хвостовая нумерация доросла до W2-14. Закрыты позже шапки:
> W2-10/W2-11 (2026-07-16), W2-13/W2-14 (2026-08-09), W2-8 (2026-08-11, map-embed).
> Открыты: W2-3 (контент-движок), W2-9 (ghost-слой — по реальному запросу студентов),
> W2-12 (part-файлы — внешний, за клиентом).

- `✅` **W2-1 · [ADD] · UX/Методист — Highlight/notes на тексте пассажа.** _(закрыто ранее,
  редизайн-трек: `PassagePane` highlight/note-аннотации на проде; баг исчезновения марок
  при ре-рендере закрыт `cb41db2`. Бэклог отставал от `main`.)_

- `✅` **W2-2 · [ADD] · Методист — Микро-урок/стратегия под тип перед дриллом.** _(закрыто
  фазой 3 Practice: P2a strategy hints — свёрнутый блок стратегий по qtype в practice-раннере,
  `c09bd76`. Контентная глубина (ловушки NOT GIVEN и т.п.) расширяема в `strategy-hints.ts`.)_

- `☐` **W2-3 · [ADD] · PM — Контент-движок: ритм пополнения + витрина «новое».**
  Библиотека тонкая (~13 Reading, часть — демо-мусор); **2026-07-10 каталог обнулён целиком**
  (плановый вайп под перезаливку клиентом, BRIEF §12.3 п.3), **2026-08-09 — повторный
  ПОЛНЫЙ прод-вайп** (каталог+прогресс+аккаунты кроме 2 админов; vocab/W/S-задания целы) —
  стартуем с нуля, ждём перезаливку клиентом. · Почему:
  test-prep удерживает объёмом и свежестью; «закончился контент» = отток. Импорт-бот уже
  есть — нужен процесс + витрина «новое на неделе». · Усилие: S (витрина) / процесс
  отдельно. · Метрика: weekly retention.

- `✅` **W2-3b · [IMPROVE][S] · Eng/Методист — Обязательный QTYPE в authoring-спеке + перезалив
  8 listening-тестов без questionType (ТЕХДОЛГ P1, 2026-07-09).**
  _(2026-07-11: hard-block вернут — публикация блокируется, если у любого вопроса пустой ИЛИ
  нераспознанный qtype (`isUnresolvedQuestionTypeWarning`, `src/lib/content/publish.ts`); добавлена
  `docs/authoring-spec.md` для клиента с обязательным QTYPE. Старые 8 listening-тестов не
  актуальны — вайп 2026-07-10 удалил их вместе со всем каталогом, «перезалив» закрыт общей
  перезаливкой.)_

- `✅` **W2-4 · [IMPROVE] · UX — Каталог: информативные карточки + дедуп демо-контента.**
  _(2026-07-07, `46f07f7`: Q-count (уже был), бейджи «Done · raw» / «Resume · progress» из
  submitted-окна, «Weak spot»-бейдж по слабейшим типам, клиентский поиск по названию.
  Демо-мусора в published не оказалось — фильтр `status='published'` чист.)_

- `✅` **W2-5 · [IMPROVE] · Методист — Геймификация в пользу учёбы.**
  _(2026-07-08, волна «учебная петля» `ab3a508`/`2962ebc`: критерии `mistakes_closed`
  (тиры 5/15/40) + `weak_type_cleared` (≥5 закрытий одного qtype) считаются из
  `mistake_resolution`; закрытия приходят из «Mark learned» и SR-graduation (3×good,
  BRIEF §12.2 п.2). Анти-накрутка: due-гейт + SR-строки только для реально проваленных
  вопросов (`dffeaf4`). Стрик за учёбу отложен осознанно — нужен day-tracking, YAGNI.)_

- `✅` **W2-6 · [IMPROVE] · Senior Eng — Страховка под нагрузку + edge-кэш контента.**
  _(2026-07-07, `15bd625`: published passage-HTML/вопросы/band-шкалы/мета вынесены в
  tagged `unstable_cache` (`src/lib/content/exam-content.ts`), сброс по тегу на
  publish/unpublish/реимпорт/audio-attach. answer_key в кэш не попадает, `enforceAccess`
  и user-чтения — вне кэша на каждый запрос, middleware не тронут. Adversarial-ревью:
  SHIP. Замер под нагрузкой на проде — за владельцем.)_

- `✅` **W2-7 · [IMPROVE] · UX — Exam-вёрстка: убрать пустой проём текст↔вопросы.**
  _(2026-07-07, `95ec8a9`: панель пассажа капнута `max-width:900px` по ширине читаемой
  колонки на десктопном брейкпоинте — пустой gutter до разделителя закрыт, мобильные табы
  и ширина текста не тронуты.)_

- `✅` **OPS-1 · [CHORE][S] · Eng — Зачистка Storage-сирот после контент-вайпа 2026-07-10.**
  _(2026-07-11, `scripts/storage-orphans.ts`, gitignored: dry-run/`--delete`, grace-период
  против гонки с runner-импортом (аудио заливается ДО строки content_item). 49 объектов /
  758.5 MB удалены — Storage 762→3.4 MB из 1024. Заодно кап 15 MB на mp3 при импорте
  (`src/lib/import/audio-cap.ts`), чтобы не упереться снова; позже ужат до 12 MB —
  действующее значение см. CLAUDE.md §Import pipeline.)_

- `✅` **W2-8 · [ADD][M] · Eng/Методист — Карта listening в practice-панели (Day 6
  Full Test 6).** _(2026-08-11: карта Day 6 оказалась не SVG, а CSS-рисунком
  (`.map-layout .mp`, позиции в inline-style + док-уровневом `<style>`) — общая
  гигиена и QuestionHtml снимали и то и другое → в verbatim-панели текстовая каша.
  Теперь `captureListeningPart` (шаг 6) пакует рисунок в самодостаточный
  srcdoc-документ (`data-map-doc` embed-узла в questions_html), рендер —
  `MapEmbed` в sandbox-iframe БЕЗ allow-scripts (стили изолированы, скриптов нет
  по построению). Своя гигиена `stripMapEmbedLeaks` (inline-style жив), leak-скан
  фрагмента до сериализации, CSS проверен на прямой текст ответа (комментарии
  стрипаются), кап 200KB. Работает в practice И в диплинке mistake-дрилла;
  mock-iframe не тронут; легаси `.map-stage` (base64-img) осознанно остаётся
  вырезаемым. **CSS источника санитизируется пересборкой из allowlist'а**
  (`map-embed-css.ts`) — блоклист проиграл пяти кругам ревью; инвариант
  «декодированный токен с остаточным `\` отвергается» единый для селекторов,
  значений, инлайн-стилей и `@media`-преамбул. Live-верификация на Day 6: 387 правил,
  2 media-блока, coverage всех 4 частей, 0 варнингов; **живой рендер в Chromium
  сверен с оригиналом — геометрия ±1px по 10 группам элементов, 0 сетевых запросов,
  0 скриптов в документе**.)_

- `☐` **W2-9 · [ADD][S] · UX — Practice-прогоны на Trajectory ghost-слоем.** Полые маркеры
  без линии и без влияния на прогноз, тогл в легенде. Mock-only фильтр — решение владельца
  2026-07-15 (`29a243f`); слой делать только по реальному запросу студентов.

- `✅` **W2-10 · [IMPROVE][XS] · UX — Тоглы легенды Trajectory при одной секционной линии.**
  _(2026-07-16, `8437493`: тогл рендерится для каждой секции со своей линией, не только
  когда есть обе; секция без линии — статичный ключ; note называет только нарисованное.)_

- `✅` **W2-11 · [IMPROVE][XS] · UX — Forecast: «too early to call» вместо коридора во всю
  шкалу.** _(2026-07-16, `8437493`: интервал шире 3 полос → текст вместо чисел
  (`WIDE_INTERVAL_BANDS`); математика прогноза не тронута. Той же волной: бейдж «↗/↘ N»
  на клампнутом прогноз-стабе (`b009667`+`8437493`) и снят невалидный `height="auto"`
  с `<svg>` (4 консольные ошибки/загрузку).)_

- `☐` **W2-12 · [ADD][S] · PM/Content — Полка клиента: part-файлы Listening под
  бесплатный уровень.** Part-уровень Listening (`part_1`..`part_4`, tier basic, волна
  2026-07-16..17) наполняется только реальными part-файлами от клиента —
  `parse-listening.ts` детектит одночастную разметку автоматически, но контента с ней
  пока не залито. Спека для клиента — `docs/authoring-spec.md`. · Усилие: внешний
  (клиент заливает контент), код готов.

- `✅` **W2-13 · [IMPROVE][S] · Eng — ТЕХДОЛГ: DB unique-constraint на
  `passage(content_item_id, "order")`.** _(2026-08-09, `dfb613b`: миграция 0062
  `passage_content_item_order_key` применена на прод после проверки на дубли (0 групп);
  malformed full-Reading импорт с повторным `data-part` теперь fail-closed, не тихая
  порча. Codex-ревью волны: SHIP.)_

- `✅` **W2-14 · [DIFFERENTLY][S] · PM/Eng — Решить судьбу вестигиальной
  trial-механики.** _(2026-08-09, решение владельца: монетизация = капы, trial не
  вернётся → выпилена целиком. `trial.ts`/`hasConsumedTrial`/isTrial-ветка удалены,
  `FULL_CATEGORIES` переехал в `categories.ts`, миграция 0063 дропнула `trial_claim`
  (7 исторических строк, дампы 30д), signup-копия «first full test is free» заменена
  честной. Codex: критика плана GO-with-changes + финал-ревью волны.)_

---

## Anti-scope — НЕ делать сейчас (тоже часть ответа «что делать»)

- **Расфриз AI Writing/Speaking** — заморожен осознанно (`phase-3-ai-frozen`); аудиторию
  набирают на Reading/Listening, AI — апсейл-крючок. Не трогать.
- **Полный re-grade с версионированием** — деструктивный ре-импорт уже заблокирован
  (`RegradeRequiredError`); под текущий объём контента не нужен.
- **Новые runtime-зависимости** — всё выше делается на текущем стеке (inline-стили/токены).
  Не вводить chart-/i18n-/прочие либы.
- **Тёмная тема** — РАЗРЕШЕНО 2026-08-09: dark-токенов в `globals.css` больше нет
  (вычищены попутными волнами), удалять нечего; вопрос закрыт до реального запроса темы.
- **i18n-фреймворк (next-intl)** — отдельная большая волна; для W1-6 достаточно свести
  строки к EN. Не поднимать фреймворк ради ~20 строк.

---

## Гипотезы — проверить ДО финальной приоритизации (могут переставить очередь)

- `✅` **H1 — Реальная реферал-награда. РАЗРЕШЕНА: награда = XP, НЕ Premium**
  (`referral.ts:41` → `reward: "xp:inviter=100,invitee=50"`). UI врал («one week of Premium») —
  исправлено на правду в W1-6 (`d19046d`, все 3 инстанса: invite/profile/pricing-FAQ). Вывод:
  W1-7 НЕ прыгал на 1-е место (фарм даёт дешёвый XP, не слив Premium).
- `—` **H2 — Даёт ли бесплатный Full-тест band + разбор free-юзеру** («Free first full test»).
  СНЯТА 2026-08-09: trial-механика удалена (W2-14) — все full-моки бесплатны под
  недельным капом, «единственного бесплатного Full» больше не существует.
- `✅` **H3 — Качество объяснений/evidence.** РАЗРЕШЕНА 2026-08-11 оффлайн-аудитом
  8 исходников `HTML READING/` (каталог пуст — парсер прогнан по файлам клиента напрямую):
  объяснения НАСТОЯЩИЕ, не пересказ ключа. Покрытие 100% (expl+evidence на каждом вопросе
  всех боевых файлов); сильнее всего FALSE/NOT GIVEN — называют противоречие или ловушку
  («came to the same conclusion — they agree», «appear only as a comparison example»),
  evidence везде несёт параграф + дословный сниппет. Слабое место: MCQ/matching_headings
  объясняют только правильный ответ, дистракторы не разбираются — принято (уровень
  «хороший разбор», не «урок»). Побочные находки: «Full Test Template» без объяснений
  (заготовка — НЕ заливать), «COLOR PREVIEW» — дубль Animals (НЕ заливать); парсерная
  щель промптов → пункт в Парсинг-треке.
- `✅` **H4 — Мобильная реальность `/app`.** РАЗРЕШЕНА 2026-07-11 (mobile release-gate,
  BRIEF §12.3): Playwright-эмуляция (390px) прошла 3 golden path чисто, но живой проход
  владельца на реальном телефоне (375px) нашёл 2 major, которых эмуляция не увидела
  (touch-gated ветки — см. память `browser-pane-no-touch-emulation`): плавающий тулбар
  аннотаций перекрывал текст в mock-режиме, широкая matching-таблица утаскивала
  инструкции горизонтальным скроллом. Оба закрыты (`77b8220`, `be747df`). Вывод:
  viewport/overflow-находки эмуляция даёт честно, touch-gated — только реальное
  устройство или CDP `pointer:coarse`-форс.
- `☐` **H5 — Listening-флоу** (single-pass аудио, map-labelling). Не проходил вживую.
  *Проверка:* прогон Listening-теста на проде.

---

## Парсинг-трек — хвосты (2026-07-21)

> Контекст: клиент зафиксировал окончательный формат reading-HTML («Inspera Style»);
> совместимость/security/mock/verbatim-in-practice закрыты одним днём
> (`7781435..ac16a90`, детали — CLAUDE.md §Статус и память `parsing-focus-2026-07-21`).
> Здесь — осознанно отложенное.

- `☐` **[ADD] Drag-слоты для verbatim-practice** `M/L` — Inspera-тесты с DnD-вопросами
  (matching headings / sentence endings) фоллбэчат в атомизированный вид, т.к.
  drag-вопросы не имеют `.q-slot`. Вариант C аудит-карты: слоты/групп-рубрики для
  drag-вопросов в capture + рендер. Метрика: рост балла (фиделити практики).
  *Триггер:* поток клиентских DnD-тестов + жалоба на вид практики.
- `☐` **[IMPROVE] Голое «Matching» → решение владельца** `S` — сейчас тихо мапится в
  `matching_info` с low-confidence варнингом; альтернатива — unknown → hard-block
  (форсит authoring-фикс). Одна строка после решения.
- `☐` **[IMPROVE] XP-гейт повторных сдач** `S` — XP (`10+rawScore`) начисляется за
  любую пересдачу; рейтинг/лидерборд/бейджи защищены (первая сдача), XP — косметика.
  Осознанно НЕ гейчен 2026-07-21 (вовлечённость); включить, если появится
  XP-ранжирование. Сигнал первой попытки уже в транзакции (`c.n===1`).
- `✅` **[IMPROVE] Prompt-capture: `.mcq-stem` и `.summary-line` в single-passage** `S` —
  _(найдено H3-аудитом и закрыто 2026-08-11, `9a9444a`: `promptOf` предпочитает
  собственный `.mcq-stem` блока рубрике группы; completion сужается до своей строки,
  когда контейнер с id держит больше одного поля (`p.summary-line` группы 20–22),
  видимый номер `.qn` вычищается. Попутно голое поле без `.blank-wrapper` получило
  маркер `____` — и гейт `empty prompt` стал считать длину БЕЗ маркера: четыре
  подчёркивания ровно добивали до порога и отключили бы предупреждение (поймано
  существующим тестом). Practice/mock не затронуты — там verbatim-панель.)_
- `✅` **[IMPROVE] Dependabot moderate-хвост** `M` — _(2026-08-09: рассосался без
  breaking-апгрейдов — транзитивные lockfile-обновления (postcss 8.5.x и далее) закрыли
  advisories; полный `npm audit` (включая dev) — 0 уязвимостей.)_

## Сводка (волнами)

| Волна | Пунктов | Суть |
|---|---|---|
| 1 — деньги сейчас | 7 ✅ | **ЗАКРЫТА (2026-06-18):** пейволл, онбординг+мини-диагностика, result-урок, band, шеринг, EN+pricing, анти-бот+identity. Вынесен W1-5b student-бот → после Волн 2–3. |
| 2 — ров и глубина | 15 (12 ✅) | highlight, стратегия-урок, каталог, геймификация-в-учёбу, eng-страховка, exam-вёрстка, QTYPE hard-block, OPS-1, тоглы+forecast Trajectory, unique-constraint passage, trial выпилен, map-embed; **открыты: W2-3 контент-движок, W2-9 ghost-слой, W2-12 part-файлы** |
| Anti-scope | 5 | AI, re-grade, deps, dark-theme, i18n-фреймворк |
| Гипотезы | 5 (H1 ✅, H3 ✅, H4 ✅, H2 снята) | реферал-награда ✅, free-Full снята (trial удалён 2026-08-09), объяснения ✅ (2026-08-11, оффлайн-аудит), mobile ✅ (2026-07-11), Listening |
| Структурный must (вне скоринга) | 1 ✅ | mobile release-gate закрыт предзапусковой волной 2026-07-11 (BRIEF §12.3) |
